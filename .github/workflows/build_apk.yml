name: Build Flutter APK

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Java JDK
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Setup Flutter SDK
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.22.x'
          channel: 'stable'

      - name: Re-generate Flutter Project Files
        run: flutter create . --platforms=android --project-name eta --org com.renantefullo.navmaster

      - name: Install Dependencies
        run: flutter pub get

      # FIX FOR ICON: Awtomatikong papalitan ang Flutter icon gamit ang icon.png mo
      - name: Generate App Launcher Icons
        run: flutter pub run flutter_launcher_icons

      - name: Build APK (Release)
        run: flutter build apk --release

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: NavMaster-ETA-Pro-APK
          path: build/app/outputs/flutter-apk/app-release.apk
